{
  "name": "Pygbag Dev",
  "build": { "dockerfile": "Dockerfile" },
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  },
  "postCreateCommand": "git lfs install && pip install -r requirements.txt",
  "forwardPorts": [8000]
}